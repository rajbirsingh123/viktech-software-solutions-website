import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Viktech Software Solutions",
  description:
    "Viktech Software Solutions builds web apps, mobile apps, and cloud systems for startups and growing companies.",
  metadataBase: new URL("https://viktechsoftware.com"),
  openGraph: {
    title: "Viktech Software Solutions",
    description:
      "Custom web, mobile, and cloud software for startups and growing companies.",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
